<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Officer / Support Staff: full hr.employee (Odoo default action). -->
    <record id="hr.menu_hr_employee_payroll" model="ir.ui.menu">
        <field name="group_ids" eval="[(6, 0, [ref('hr.group_hr_user'), ref('hr_employee_hrm_detail.group_hr_employees_supporter')])]"/>
    </record>

    <!-- Employee (staff): public profiles only — same-store filter via hr.employee.public. -->
    <menuitem id="menu_hr_employee_staff"
              name="Employees"
              parent="hr.menu_hr_root"
              action="hr.hr_employee_public_action"
              groups="hr_employee_hrm_detail.group_hr_employees_staff"
              sequence="3"/>
</odoo>
